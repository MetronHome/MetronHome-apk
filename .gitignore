# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# Lockfiles: npm is the package manager (bun.lock / bun.lockb are scaffolding leftovers)
bun.lock
bun.lockb

dist
dist-ssr
dev-dist
*.local

# Android signing (do NOT commit the keystore or its properties)
android/keystore.properties
*.keystore

# Windows-side build artifacts (Android build runs on the Windows FS via bash.exe)
metronhome-android/
metronhome-*.aab
metronhome-*.apks
metronhome-apks-extract/
mh-apks/

# Windows/WSL download artifacts
*Zone.Identifier


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Brainstorm visual companion (mockups temporaires)
.superpowers/
